const fs = require('fs'); const raw = fs.readFileSync('sd_current.json', 'utf16le').replace(/^\uFEFF/, ''); const json = JSON.parse(raw); const rows = json.data; const target = rows.findIndex(r => r.some(c => typeof c === 'string' && c.toLowerCase().includes('alliance'))); if (target !== -1) { for (let i = target; i < target + 5; i++) { if (rows[i]) console.log(JSON.stringify(rows[i])); } }
